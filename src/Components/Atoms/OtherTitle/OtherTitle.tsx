import style from './otherTitle.module.scss'

const HomeTitle = () => {
    return(
        <div className={`${style.typewriter}`}>
            <h1>Projects</h1>
        </div> 
    )
}

export default HomeTitle